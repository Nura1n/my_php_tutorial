<?php

    $radius = 25;
    $pi = 3.14;

    //echo $pi * $radius ** 2;

    //echo 2 * (4 + 9) / 3;

    // increment & decrement operators

    //echo $radius--;
    //echo $radius;

    // shorthand operators

    $age = 20;

    //$age -= 10;
    //$age *= 2;
    //echo $age;

    // number functions

    //echo floor($pi);

    //echo ceil($pi);
    echo pi();


?>

<!DOCTYPE html>
<html>
<head>
    <title>PHP Tutorials</title>
</head>
<body>


</body>
</html>

//nota

integers (whole number)

float/double (decimal)

basic operator - *, /, +, -, ** (find power of something)

order of operation ( B I D M A S ) (bracket, power of, division, multiplication, subtraction, addition)

increment (add to a number using ++)

decrement (minus to a number using --)

shorthand operators

+= (adding a variable)
*= (multiply a variable)

function

floor = take the number/float and it flowed to the nearest interger
ceil = take the value and move up to the nearest integer
pi = is well pi